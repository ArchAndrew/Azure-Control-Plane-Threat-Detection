// Monitor access to Azure Key Vault secrets

AzureDiagnostics
| where ResourceType == "VAULTS"
| where OperationName contains "Secret"
| project TimeGenerated, OperationName, CallerIPAddress, identity_claim_upn_s, ResultType, Resource
| order by TimeGenerated desc
